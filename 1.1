library(gWidgets)
options("guiToolkit"="RGtk2")
###########################################################
w = gwindow("2D Tour plot example")
 g = ggroup(cont = w, horizontal = FALSE)
defHandler = function(h,...) print("hi")

mbl = list(
   File = list(
     openFile = list(handler=defHandler, icon="open"),
     close = list(handler=function(h,...) dispose(w), icon="cancel")
     ),
   Edit = list(
     paste = list(handler=defHandler),
     copy =  list(handler=defHandler)
     )
   )
tbl = list(
   new = list(handler = defHandler, icon="new"),
   quit = list(handler= function(h,...) dispose(w), icon="quit"),
   save = list(handler= defHandler, icon="save"),
   ok = list(handler=function(h,...) animate_xy(flea[,2:6]), icon="ok")
   )

 
if(options("guiToolkit") == "RGtk2") {
   cont = w
 } else {
   cont = g
 }

 mb = gmenu(mbl, cont=cont) 
 tb = gtoolbar(tbl, cont=cont)

vbox = glayout(cont=w)
defHandler = function(h,...) print(svalue(h$obj))
short = c("Grand","Guided","Little")
long = c("Varible 1","Varible 2","Varible 3","Varible 4","Varible 5")


vbox[1,1, anchor=c(-1,0)] <- "Variable select"
vbox[1,2] <- gcheckboxgroup(long, horizontal=FALSE, cont=vbox, handler
   = defHandler)

vbox[1,3, anchor=c(-1,0)] <- "Select Tour "
vbox[1,4] <- gradio(short, cont=vbox, handler = defHandler)


g= ggroup(horizontal = FALSE, cont=w)
vbox[2,1, anchor=c(-1,0)] = ggroup(cont=g, expand=TRUE, horizontal = FALSE)
buttonGroup = ggroup(horizontal = TRUE, cont=g)
   helpButton = gbutton("help", cont=buttonGroup)
   addSpring(buttonGroup)
   pauseButton = gbutton("Pause",cont=buttonGroup)
   addSpace(buttonGroup, 10)
   okButton = gbutton("ok", cont=buttonGroup)
   addSpace(buttonGroup, 10)
   cancelButton = gbutton("cancel", cont=buttonGroup)





















#########################################################################sss

######################choose several variables
tbl = glayout(cont=w)
defHandler = function(h,...) print(svalue(h$obj))
long = c("Varible 1","Varible 2","Varible 3","Varible 4","Varible 5")
tbl[1,1, anchor=c(-1,1)] <- "Variable select"
tbl[2,1] <- gcheckboxgroup(long, horizontal=FALSE, cont=tbl, handler
   = defHandler)

vbox = glayout(cont=w)
defHandler = function(h,...) print(svalue(h$obj))
long = c("Varible 1","Varible 2","Varible 3","Varible 4","Varible 5")
vbox[1,1, anchor=c(-1,1)] <- "Variable select"
vbox[2,1] <- gcheckboxgroup(long, horizontal=FALSE, cont=vbox, handler
   = defHandler)

######
tbl[2,2] <- (t2 <- gtable(long, multiple=TRUE, cont=tbl,
   handler=defHandler))
   size(t2) <- c(100,200)
tbl[2,3] <- gdroplist(long, editable=TRUE, cont=tbl, handler=defHandler)
visible(tbl) <- TRUE

########################## add toolbar
w = gwindow("2D Tour plot example")
g = ggroup(horizontal = FALSE, cont=w)

l <- glabel("Put widget here", cont=g, expand = TRUE)
   size(l) <- c(400,300)
######
g = ggroup(cont=g, expand=TRUE, horizontal = FALSE)
buttonGroup = ggroup(horizontal = TRUE, cont=g1)
   helpButton = gbutton("help", cont=buttonGroup)
   addSpring(buttonGroup)
   okButton = gbutton("ok", cont=buttonGroup)
   addSpace(buttonGroup, 10)
   cancelButton = gbutton("cancel", cont=buttonGroup)

########add menubar and toolbar
w = gwindow("2D Tour plot example")
 g = ggroup(cont = w, horizontal = FALSE)
defHandler = function(h,...) print("hi")
 mbl = list(
   File = list(
     openFile = list(handler=defHandler, icon="open"),
     close = list(handler=defHandler, icon="cancel")
     ),
   Edit = list(
     paste = list(handler=defHandler),
     copy =  list(handler=defHandler)
     )
   )
 tbl = list(
   new = list(handler = defHandler, icon="new"),
   quit = list(handler= defHandler, icon="quit"),
   save = list(handler= defHandler, icon="save")
   )
if(options("guiToolkit") == "RGtk2") {
   cont = w
 } else {
   cont = g
 }

 mb = gmenu(mbl, cont=cont) 
 tb = gtoolbar(tbl, cont=cont)

######
if(options("guiToolkit") == "RwxWidgets") {
   cont = w
 } else {
   cont = g
 }

####add something under
g1 = ggroup(cont=g, expand=TRUE, horizontal = FALSE)
 l <- glabel("This space for rent", cont=g1, expand=TRUE)
 size(l) <- c(300,200) 



#############################choose one variable
tbl[1,1, anchor=c(-1,1)] <- "Select variable you want to display"
tbl[1,2] <- gradio(short, cont=tbl, handler = defHandler)
tbl[1,3] <- (t1 <-gtable(short, cont=tbl, handler = defHandler))
size(t1) <- c(100,200)
tbl[1,4] <- gdroplist(short, cont=tbl, handler = defHandler)


###############

 g2 = ggroup(cont=g, expand=TRUE, horizontal = FALSE)
 l <- glabel("This space for plot", cont=g2, expand=TRUE)
 size(l) <- c(300,200)           # o/w buttons expand


tbl <- list("Quit"=list(handler=function(h,...) dispose(w),
   icon="cancel"))
   mbl <- list("File"= list(
     quit = list(handler=function(h,...) dispose(w),icon="cancel"),
     sayHi = list(handler = function(h,...) print("hi"))
    ))

 tbl = list(
   new = list(handler = defHandler, icon="new"),
   quit = list(handler= function(h,...) dispose(w), icon="quit"),
   save = list(handler= defHandler, icon="save")
   )

mbl = list(
   File = list(
     openFile = list(handler=defHandler, icon="open"),
     close = list(handler=function(h,...) dispose(w), icon="cancel")
     ),
   Edit = list(
     paste = list(handler=defHandler),
     copy =  list(handler=defHandler)
     )
   )
###################
mbl = list(
   File = list(
     openFile = list(handler=defHandler, icon="open"),
     close = list(handler=defHandler, icon="cancel")
     ),
   Edit = list(
     paste = list(handler=defHandler),
     copy =  list(handler=defHandler)
     )
   )
 tbl = list(
   new = list(handler = defHandler, icon="new"),
   quit = list(handler= defHandler, icon="quit"),
   save = list(handler= defHandler, icon="save")
   )


