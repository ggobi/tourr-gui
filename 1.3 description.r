source(file.choose())                 #######load load.r from tourr
ls()
animate_xy
grand_tour
animate_xy(flea[,2:6])


##########?ggroup
1.To have the widget grow to fill the space, use the argument expand=TRUE. 
2.To anchor the widget into a portion of the space provided, 
use the argument anchor=c(a,b) where a and b are values in -1,0,1 
and specify the position using Cartesian coordinates. 
3.The containers pack in child widgets from either left to right (when horizontal=TRUE) 
or from top to bottom (when horizontal=FALSE). 

